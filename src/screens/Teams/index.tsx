import { Container, HeaderContainer, Title } from "./styles";
import { Header } from "@components/Header";

export const Teams = () => {
  return (
    <Container>
      <HeaderContainer>
        <Header />
      </HeaderContainer>
    </Container>
  );
};
